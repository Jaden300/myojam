services:
  - type: web
    name: myosignal-api
    runtime: python3
    buildCommand: pip install -r requirements.txt && python backend/build.py
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0